# Google Play (supply) defaults
# https://docs.fastlane.tools/actions/upload_to_play_store/

json_key_file ENV.fetch("PLAY_STORE_JSON_KEY", File.expand_path("play-store-key.json", __dir__))
package_name ENV.fetch("ANDROID_PACKAGE_NAME", "com.thcathy.earntimetoplay")
metadata_path File.expand_path("metadata/android", __dir__)

skip_upload_apk true
# Lane options set skip_upload_aab false when an AAB is passed.
